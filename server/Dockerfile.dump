FROM myt/server

USER root

COPY \
    dump/.dump.sql  \
    dump/beforeDump.sql \
    dump/afterDump.sql \
    dump/

RUN gosu mysql docker-init.sh

RUN echo "[LOG] Import finished." \
    && rm -rf /workspace

USER mysql
